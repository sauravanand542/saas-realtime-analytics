FROM apache/spark:3.5.5

USER root
RUN apt-get update \
    && apt-get install -y --no-install-recommends python3-pip \
    && python3 -m pip install --no-cache-dir duckdb==1.5.5 snowflake-connector-python==4.7.5 \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /opt/project
ENV PYTHONPATH=/opt/project
